@model IEnumerable<HodApiFront.Models.MapPlayerGuild>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Guild.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Player.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Guild.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Player.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MapPlayerGuildId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MapPlayerGuildId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MapPlayerGuildId })
        </td>
    </tr>
}

</table>
