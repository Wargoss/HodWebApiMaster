@model IEnumerable<HodApiFront.Models.MapPlayerPurchase>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Player.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Purchase.PurchaseId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Player.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Purchase.PurchaseId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MapPlayerPurchaseId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MapPlayerPurchaseId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MapPlayerPurchaseId })
        </td>
    </tr>
}

</table>
