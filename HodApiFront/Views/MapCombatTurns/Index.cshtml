@model IEnumerable<HodApiFront.Models.MapCombatTurn>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Combat.CombatId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Turn.TurnId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Combat.CombatId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Turn.TurnId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MapCombatTurnId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MapCombatTurnId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MapCombatTurnId })
        </td>
    </tr>
}

</table>
