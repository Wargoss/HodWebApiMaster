@model IEnumerable<HodApiFront.Models.MapPlayerCharacter>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Character.CharacterId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Player.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Character.CharacterId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Player.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MapPlayerCharacterId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MapPlayerCharacterId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MapPlayerCharacterId })
        </td>
    </tr>
}

</table>
